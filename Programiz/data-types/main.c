#include <stdio.h>
#include <stdlib.h>

int main() {
    // double and float data types
    // double takes 8 bytes of memory and float takes 4 bytes of memory.
    // double has a precision of 15-16 digits while float has a precision of 6-7 digits.

    double num = 12.45;
    printf("value of double num is %lf\n", num);
    
    return 0;
}